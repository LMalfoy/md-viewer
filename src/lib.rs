//! Native, read-only Markdown viewer application components.

/// egui application state and rendering.
pub mod app;
/// Command-line argument parsing.
pub mod args;
/// Markdown file validation and loading.
pub mod document;
